<%layout('layouts/boilerplate')%>
    <h1> alll campgrounds</h1>
    <ul>
        <a href="campgrounds/new">Add new camp</a>

    <% for (let campground of campgrounds){ %>
    
            <li><a href="campgrounds/<%= campground._id%>"> <%= campground.title%></a></li>
    <% } %>
</ul>
